# Опційні залежності для експериментів, не потрібні для роботи сервісу.
#
#   pip install -r requirements-local.txt
#
# faiss-cpu — альтернативний бекенд пошуку (`--faiss`). На корпусі цього
# масштабу приросту не дає (див. README), стоїть тут лише щоб інтерфейс можна
# було перевірити на реальній бібліотеці, а не на numpy-заглушці.
#
# sentence-transformers переїхав у requirements.txt: e5-small тепер робоча
# модель сервісу, а не деталь одного експерименту.

faiss-cpu>=1.8
